export type MikRouterType = {
    id: string
    host: string
    username: string
    password: string
    user_id: string
}

export type MikRouterLogType = {
    id: string
    time: string
    topics: string
    message: string
    router_id: string
}